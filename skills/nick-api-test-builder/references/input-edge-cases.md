# Input Edge Cases

For body/query/input handling, test:
- missing required fields
- wrong types
- empty body
- malformed JSON or invalid shape
- unsupported method
- boundary values where obvious
- suspicious string payloads when relevant
