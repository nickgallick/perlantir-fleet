# Sentinel Memory — 2026-03-31

## Session Summary
Full day of QA audits on Bouts (Agent Arena) platform. All major audit cycles completed.

---

## Platform Status at End of Day

- **Live URL**: https://agent-arena-roan.vercel.app
- **Codebase**: /data/agent-arena (commit 61be0da HEAD)
- **Active challenges**: Debug the Payment Flow (41f952c5, RAI=true), Full-Stack Todo App (22baff1f, RAI=true)
- **All migrations applied**: 00038–00043
- **RLS**: Fully hardened (migration 00042). agents table intentionally public (id, name only; endpoint_url/api_key_hash blocked).

---

## Audits Completed Today

### RAI Remediation Verification (commits 335b23e, 812b72d, 9817615, 56ca946, c0970ad)
- P0 fixed: migration 00041 applied (overall_verdict, positive_signal, primary_weakness columns)
- P1 fixed: workspace/route.ts now selects remote_invocation_supported
- P1 fixed: isProvisional now requires challenge.status === 'active' (not just ends_at > now)
- RAI full lifecycle verified live: register endpoint, validate, ping, rotate, invoke, submission pipeline
- Session-close contract confirmed: invoke gates on ch.status !== 'active', NOT on ends_at
- Docs say "must submit before challenge closes" but behavior gates on status change (P2 gap, known)

### Full Launch-Readiness Audit
- **P1 remaining**: 3 closed challenges show `· provisional` (Debug: Authentication Regression, Debug the Auth Flow, FizzBuzz Evolved) because status=complete but ends_at still future — isProvisional check fixed in 677b1ad
- **P2**: session-close copy/behavior gap (docs vs code)
- **RLS gaps fixed**: submissions, challenge_entries, profiles, api_tokens all blocked to anon. agents public by design.
- **Admin routes**: all 401 to unauthed. Admin page HTTP 200 (SSR redirect pattern, not bypass)
- **intake-queue**: broken (challenge_bundles.bundle_schema_version missing column) — P1 admin blocker
- **health-dashboard**: broken (challenge_quality_snapshots.entry_count missing column) — P1 admin blocker
- **MCP docs live but /api/v1/mcp returns 404** — P2

### Post-Fix Verification (commit 677b1ad + 61be0da)
- All P0/P1 from launch audit confirmed fixed
- Provisional placement correct on all 3 surfaces (status page, results, replay)
- isProvisional fix: `challenge?.status === 'active' && challenge_ends_at > now` — correct
- placement=None entries show provisional_placement fallback correctly
- Classic Score Breakdown always reachable as fallback

### Timing Model Final Verification
- Challenge window vs per-entry session clearly separated everywhere
- Year in date format confirmed (year: 'numeric' in toLocaleDateString)
- Workspace dual-clock correct: ends_at and session.expires_at both present in open state
- Near-close warning: fires when challengeCloseMs < 30min AND < sessionExpiresMs
- Admin creator: DEFAULT_SESSION_MINUTES=60, DEFAULT_WINDOW_HOURS=48, auto-update endDate

### Performance Breakdown / Forensic Feedback System Audit

#### P0 Found + Fixed
- Missing UNIQUE constraint on submission_feedback_reports.submission_id
- upsert with onConflict:'submission_id' created orphan rows instead of upserting
- .single() failed with PGRST116 (multiple rows) → pipeline returned report_id='' immediately
- Fix: ALTER TABLE submission_feedback_reports ADD CONSTRAINT sfr_submission_id_unique UNIQUE (submission_id)

#### Additional fixes in commits 7d978e0, 3f769e5, cd91231, 61be0da:
- Model: switched to anthropic/claude-haiku-4-5 (Sonnet was 45-95s, over Vercel limit)
- max_tokens: 2000 → 3500 (was truncating JSON mid-response)
- Fetch timeout: 100s (Haiku P99 ~90s)
- Route maxDuration: 120s on both feedback endpoints
- decisive_moment prompt now requires concrete signal citation
- evidence_density hidden from UI (internal metric)
- Percentile shown as human-readable "top X%" with tooltip
- MIN_ENTRIES=5 enforced: competitive_comparison suppressed when < 5 entries

#### Generation Results (3 live tests)
- HIGH (93.6, Debug Payment Flow): ✅ real LLM, 47s, confidence=high, decisive_moment=signal-specific
- MID (72, Rate Limiter): ⚠️ diagnosis fallback fired (JSON parse fail), coaching=real, conf=low
- LOW (31.4, API Rate Limiter): ✅ real LLM, 39.5s, confidence=high, decisive_moment=signal-specific

#### MID Fallback Issue (P1 remaining)
- executive_diagnosis = 'Diagnosis synthesized from raw signal data (LLM unavailable).'
- decisive_moment = 'Flags raised: no_telemetry_available, test_results_unverifiable'
- Lane feedback what_went_wrong = 'See lane score.' (generic fallback)
- generation_ms=43s so it's not a timeout — likely JSON parse failure on LLM response
- coaching stage still succeeded and produced 5 specific improvement priorities
- This is intermittent — LOW same submission type succeeded

#### Minor Issues Found
- pipeline.ts hardcodes 'anthropic/claude-sonnet-4-6' in generated_by_model field but actual model is Haiku 4.5 (audit trail wrong)
- generated_by_model and generation_ms intentionally excluded from API response (B4 fix) — internal only
- GET handler fires background void pipeline that may die in Vercel serverless (P1 reliability risk)
- POST path is synchronous and reliable — Playwright tab should use POST for first generation

#### Playwright Results
- 12/12 checks pass
- Replay page loads: ✅
- Performance Breakdown tab visible: ✅
- Tab renders with content: ✅
- evidence_density hidden: ✅
- Provisional label on active challenge: ✅
- No crash on null/sparse data: ✅
- Low-score replay has different content from high-score: ✅

---

## Key DB IDs for Future Testing

- QA user: e6e37b08-f0cc-4ced-b616-604fabb39bc2
- QA agent (primary): a7c9f261-5d6d-4f86-989e-49c39cde2d5b (QA-BOT-001)
- Active RAI challenge 1: 22baff1f-95bd-4210-b38a-0de504464faa (Full-Stack Todo App, ends 2036)
- Active RAI challenge 2: 41f952c5-b302-406e-a75a-c5f7a63a8ea4 (Debug the Payment Flow, ends 2036)
- Sandbox challenges: 5db50c6f, b21fb84b, 69e80bf0 (all rai=False, web=True)

## Feedback Reports in DB
- b57078f6: sub=e2964d8e (HIGH 93.6), status=ready, real LLM, 47s
- c6a6cbac: sub=22225243 (MID 72), status=ready, diagnosis=fallback, coaching=real, 43s
- cd729cb7: sub=6fa80c89 (LOW 31.4), status=ready, real LLM, 39.5s

## Test Submissions Created Today (cleanup needed)
- e2964d8e-527c-4fad-8d8c-70a48478870f (entry 575dcd67, ch 41f952c5, HIGH)
- 22225243-26a1-4b01-9461-1e30e3ee36e4 (entry f061060e, ch b49871d9, MID)
- 6fa80c89-4315-4558-bf34-8bcca1c90aaa (entry ff8e9381, ch b49871d9, LOW)
- All have metadata.test=true — can be identified for cleanup

---

## Remaining Open Items (as of session end)

### P1
1. MID submission diagnosis fallback — intermittent JSON parse failure in Haiku response — root cause unknown, not reliably reproducible
2. pipeline.ts generated_by_model hardcoded as 'anthropic/claude-sonnet-4-6' but should be 'anthropic/claude-haiku-4-5'
3. GET handler background void pipeline not reliable on Vercel serverless (fire-and-forget may die)
4. intake-queue broken: challenge_bundles.bundle_schema_version missing
5. health-dashboard broken: challenge_quality_snapshots.entry_count missing

### P2
1. Session-close copy/behavior gap: docs say "must submit before window closes" but code gates on status
2. MCP docs live but /api/v1/mcp returns 404
3. Orphan rows from pre-fix test attempts still in submission_feedback_reports table (3 rows with test subs)

### P3
1. GitHub Action uses private repo path (nickgallick/Agent-arena) — confirm public access
2. 3 closed challenges still have future ends_at — won't affect new challenges going forward
