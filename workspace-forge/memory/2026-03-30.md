# Forge Memory — 2026-03-30

## Session Summary
Long build session — Web Submission System (Phase W) W0 through W3 completed and deployed.

## Key Decisions & Changes (2026-03-30)

### Supabase PAT rotated
- Old PAT expired: sbp_851afa9dfe477af8cb5c9a6e2c22ecdab2d5960b
- New PAT (active): sbp_f8240f4ee4a6edf7bbdb8f60aa1efa5affeb2b27
- Updated in TOOLS.md

### Web Submission System — Phase W Build Log

#### W0 — Foundation (Git: abdf3dc)
- Migration 00035 applied: web_submission_supported (challenges), submission_source (submissions), status constraint extended (workspace_open, expired added), legacy 'judging'→'in_progress'
- Sandbox challenges (all 3 fixed UUIDs) flagged web_submission_supported=true
- PATCH /api/admin/challenges/[id] accepts web_submission_supported
- Challenge Health admin tab: Web Submit toggle column

#### W1 — Participation State Model (Git: 0ccf990)
- ParticipationStatusBlock component: 7 explicit states (not_entered/entered/workspace_open/submitted/result_ready/expired/failed)
- 'judging' removed — not a real challenge_entries status
- participation_state + user_entry_id added to challenge detail API response
- web_submission_supported added to public challenge detail + list API
- workspace_open added to events/stream allowlist + JUDGEABLE_STATUSES
- workspace_open added to SUBMITTABLE_STATUSES in /api/v1/submissions/route.ts

#### W1-patch (Git: d9ec19f)
- result_ready CTA → /results (was broken /challenges/[id]/results 404)
- Dead 'judging' state removed from type + stateConfig

#### W1-patch2 (Git: f7a8bfc)
- workspace_open added to SUBMITTABLE_STATUSES in v1/submissions/route.ts (Sentinel catch)

#### W2 — Workspace Page (Git: 48903d4)
- GET /api/challenges/[id]/workspace: idempotent session creation, timer-start on first open
- /challenges/[id]/workspace page: left=prompt panel, right=timer+constraints+textarea+submit
- 7 handled states: loading/open/not_entered/already_submitted/expired/not_supported/error
- Constraints panel: collapsed default, expands to full list
- "Submitting as [Agent Name]" identity card
- 5-min warning banner (amber)
- One-submission confirm modal
- Fixed [challengeId]/sessions upsert status 'active' → 'workspace_open' (violated CHECK constraint)

#### W3 — Web Submit Route + Status Page (Git: 7104052)
- POST /api/challenges/[id]/web-submit: full pipeline wired
  - JWT auth, agent from user account
  - Dual-session conflict resolved by pre-flight query (no reliance on DB uniqueness)
  - submission_source='web' explicit
  - All errors return JSON
  - Redirects to /submissions/[id]/status
- /submissions/[id]/status: polls every 5s, 5 states, event log, 10min timeout
- Polaris applied: 'Web Submission' label, 'Your Solution' textarea, ⚠ amber danger constraints, Bot icon, confirm copy tightened, 50-char warning removed

#### W3-patch (IN PROGRESS at session compaction)
Sentinel + Aegis blockers being fixed:
1. vercel.json: /api/cron/process-judging-jobs added at */2 * * * * (was missing — web submissions stayed queued forever)
2. connector/submit: terminal status check added (blocks duplicate submissions after web submit)
3. Migration 00036: CREATE UNIQUE INDEX idx_submissions_one_per_entry ON submissions(entry_id) — applied to DB, deduped 7 existing test duplicates
4. Middleware: auth protection added for /challenges/*/workspace and /submissions/*/status routes
5. web-submit route: explicit ownership check on client-provided session_id (in progress)

### Architecture Decisions (permanent)
- Web submission timer starts on workspace open (not challenge entry) — fairness model
- User is operator, registered agent is identity — "Submitting as [Agent Name]"
- submission_source='web' stored on all web-path submissions
- No draft save in V1, no auto-resume — explicitly surfaced in UI
- Expired state copy: "This entry can no longer accept a submission"
- Post-submit destination: /submissions/[id]/status → polls real pipeline
- result_ready CTA: /results (the real dashboard, not a per-challenge route)
- One submission per entry enforced at DB level (unique index) + application level

### DB Migrations Applied Today
- 00035_web_submission.sql: web_submission_supported, submission_source, status constraint
- 00036_submissions_unique_entry.sql: dedup + unique index on submissions(entry_id)

### Current Commit State (at compaction)
- Latest clean deploy: Git 7104052 (W3)
- W3-patch in progress — not yet committed/deployed
- W4 = next after W3-patch approved: status page polish, breakdown verify, deferred Polaris card merge

### Cron Jobs (updated)
- bouts-judging-processor: was OpenClaw cron only, now ALSO in vercel.json at */2 * * * *
- OpenClaw cron ID: 4e028b13-dad0-4b36-a7f7-32dd5fdfd950 (still active)
- Both run the same /api/cron/process-judging-jobs endpoint

### Nick's Operating Rules (reinforced this session)
- One phase per session — deploy + memory + handoff + outline before proceeding
- After every deploy: update MEMORY.md AND HANDOFF.md, no exceptions
- Sentinel and Aegis clear each phase before W-increment
- W3-patch fixes are blockers — must ship before W4 approval

### Open Items After W3-patch
- W4: submission progress states polish, failed state handling, breakdown page verify for web-path
- W5: homepage + docs alignment once feature is real
- Deferred Polaris: timer + "Submitting as" card merge (W4 optional)
- Nick side: Stripe live keys, Iowa address, bouts.gg domain
