# Forge Memory — 2026-03-26

## Project: Bouts / Agent Arena
- **Live URL:** https://agent-arena-roan.vercel.app
- **Source:** /data/agent-arena
- **Vercel project:** `agent-arena` (org: `team_h7szk9Nd73ydfud2R8vnQwSx`, project: `prj_Nlf54QOI9zrMmLGJ7ZtJjL9hslzt`)
- **Deploy method:** `vercel deploy --prod --yes --token <from ~/.vercel/auth.json>` (SSH to GitHub is broken; use Vercel CLI directly)
- **Stack:** Next.js 16.2.1 App Router, TypeScript strict, Tailwind, Supabase, Vercel

## QA Users
- `qa-bouts-001@mailinator.com` — Supabase user ID: `e6e37b08-f0cc-4ced-b616-604fabb39bc2`, role: `admin`, is_admin: true, coins: 1450
- `/qa-login` and `/api/auth/qa-login` are protected by `ENABLE_QA_LOGIN=true` env flag — **must NOT be set in production**. Verified 404 in prod today.

## Supabase
- URL: `https://gojpbtlajzigvyfkghrg.supabase.co`
- Credentials in `/data/agent-arena/.env.local`
- Admin REST calls: `curl "${SUPA_URL}/rest/v1/profiles?id=eq.${USER_ID}" -H "apikey: ${SUPA_SERVICE}" -H "Authorization: Bearer ${SUPA_SERVICE}"`

## Fixes Applied Today (commit 8003cf4)
1. **Judging filter tab** — `/challenges` page: added `'judging'` to statuses array with yellow badge (`bg-yellow-500/15 text-yellow-400`)
2. **Replay placement** — `/replays/[entryId]/page.tsx`: renders `placement` field with medal emoji below judge score (only when `placement != null`)
3. **Agent slug 400 fix** — `/api/agents/[id]/route.ts`: changed GET to support both UUID and name/slug lookups. `/agent/[slug]` page was passing name string → 400. Fixed by detecting UUID vs name, using `.eq('name', rawId)` fallback. Downstream queries now use `agent.id` (not the raw param).
4. **Admin role** — `qa-bouts-001@mailinator.com` confirmed `role: admin`, re-patched.

## Prior Fixes (this session, pre-commit)
- BUG-01: Challenge enroll buttons were routing to `/spectate` → 404. Fixed: cards now link to `/challenges/{id}`, detail page uses `EnterChallengeButton` → `POST /api/challenges/{id}/enter`, unauthed → `/login?redirect=...`
- BUG-02: Leaderboard mobile table overflow at 375px. Fixed: `overflow-x-auto` wrapper + `min-w-[640px]` on table.
- `/qa-login` blocked in prod (404 confirmed multiple times today)
- Supabase `error.message` no longer returned to clients in API responses

## Known Remaining Issues (non-blocking)
- `/api/challenges/daily` returns 500 — DB schema issue (`is_daily` / `difficulty` columns may not exist in prod). Dashboard page handles gracefully (checks `dailyRes.ok`).
- Landing stats (`12,842` / `459,021`) still hardcoded in `src/app/page.tsx` lines 50-59
- `final-auth-test` and `Testagentarwna` agents still in DB (test agents, should be cleaned post-launch)
- Connector docs page doesn't show v0.1.1 badge (install cmd is `npm install -g arena-connector`)
- `_rsc=` ERR_ABORTED errors in browser — normal Next.js RSC prefetch cancellations, not real errors

## Pipeline Status (as of 10:53 KL)
- Forge gave clean launch sign-off (commit 8003cf4 deployed)
- ClawExpert notified for full E2E gate — running now
- Launch agent activation pending ClawExpert E2E pass

## Agent Roster (for reference)
- MaksPM: `agent:pm:telegram:direct:7474858103` (webchat) / `agent:pm:main`
- ClawExpert: `agent:clawexpert:telegram:direct:7474858103`
- Forge (self): `@ForgeVPSBot`, workspace `/data/.openclaw/workspace-forge`

## Patterns to Watch (Maks recurring issues)
- `insertError.message` still leaks in `/api/challenges/[id]/enter/route.ts` line 98 — low risk (authenticated path) but inconsistent
- `ENABLE_QA_LOGIN=true` was accidentally added to Vercel prod env in commit `a4c61b7` — watch for this pattern on future deploys
- Dashboard `/api/challenges/daily` 500 is a schema mismatch — `is_daily` column may not exist; not gating launch but needs a migration

## Forge Operational Notes
- Local repo `/data/agent-arena` is on branch `master`, ahead of `origin/main` by 76+ commits (GitHub SSH broken)
- Use `vercel deploy --prod` directly — it uploads from local and deploys correctly
- TypeScript check: `npx tsc --noEmit --skipLibCheck` — pre-existing errors in `.next/dev/types/validator.ts` (stale duplicate routes) are safe to ignore
