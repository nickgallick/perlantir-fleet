# Multi-Repo Workflow

## When using ~/Projects/
- confirm the target repo first
- show repo + branch + dirty/clean state
- avoid cross-repo confusion
- if multiple repos are dirty, summarize them before acting

## Good habit
Use a quick repo summary before batch git work.
